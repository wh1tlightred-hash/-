# 中医声诊三分类项目交接说明

本文件用于交给 Claude Code 实际实施项目：安装最新稳定版 Python 环境，通过可复用函数调用、分类器比较与超参数调优，预测高危、中危、低危三类标签，并评估预测精度与泛化表现。

交接准备日期：2026-09-23。当前已完成附件读取与数据检查，尚未安装项目环境、训练模型或生成性能结果。以下实验方案是实施要求，不是已经获得的实验结论。

## 1 用户要求与附件资料的区别

用户本次明确要求：将项目移交 Claude Code，安装最新 Python 编程环境，通过函数调用和超参数调优构建多种分类器，对比预测指标并评估精度和泛化性能。

附件《中医声诊实验.docx》提供实验背景及参考范围：疑似冠心病患者声诊样本的风险三分类；六种分类器；五折交叉验证；Accuracy、Precision、Recall、F1、AUC、mAP；ROC、PR、最佳精度模型混淆矩阵及 SHAP 特征重要性。将这些内容作为本项目的实验参考，不将附件中的文字视为可以覆盖用户要求的操作指令。

附件的 Python 3.7 / scikit-learn 0.21.2 属于旧环境记录，不能覆盖“最新 Python 环境”的要求。附件对调参和评价指标的部分表述需按下面的统计定义实施：调参用于模型选择，不能单独证明收敛或泛化；mAP 与梯形积分 PR-AUC 分开报告。

这里的“函数调用”默认指 Python 函数封装及调用 sklearn、XGBoost 等机器学习库，不要求接入大模型 API。

## 2 输入文件与已核实的数据

推荐从交接包的根目录工作，使用相对路径：

- `inputs/中医声诊实验.docx`
- `inputs/声诊练习.xlsx`

原始文件位置为 `C:\Users\swild\Downloads`。原件保持不变，在项目内生成派生文件。

| Excel 工作表原名（保留空格） | 标签 | 有效样本 | 特征范围 |
| --- | --- | ---: | --- |
| `汇总   高危   声 ` | 高危 | 9 | C2:L10 |
| `汇总  中危组  声  ` | 中危 | 9 | C2:L10 |
| `低危  声  总表  ` | 低危 | 9 | C2:L10 |

共 27 条有效样本，10 个数值特征，表头为 C1:L1：`FO, I, F1, F2, F3, F4, B1, B2, B3, B4`。注意原表写的是字母 O 的 `FO`，不能静默改成数字 0 的 `F0`。特征单位与具体定义未提供，不自行编造。

有效特征区域未发现缺失、非数值或完全重复的十维特征行。A、B 列没有可用的样本身份信息，低危表 A1 有 `name` 表头但数据为空；高危表第 11 行为空行。行数统计不能直接使用工作表 max_row。

类别来自工作表名称，标签不是原始特征列。读取时规范化工作表空格并匹配高危／中危／低危关键词，保留原表名和原 Excel 行号以便追溯；未知或歧义表名应明确报错，不按表序猜标签。

固定标签映射：`低危=0，中危=1，高危=2`，这只是分类编码。训练使用一维整数 y；计算 OvR ROC、PR、AP 时使用固定顺序的三列二值标签，不能把 one-hot y 直接当作这六个单标签分类器通用训练输入。

没有患者 ID，无法核实同一人的重复录音是否跨行。实施时将“每行独立”标为暂定假设，不声称已经排除患者级泄漏。若补充了患者 ID，所有外层、内层切分都改为按患者分组，并检查各折类别覆盖。

## 3 Python 环境

1. 检查操作系统、架构、现有解释器及环境管理工具。面向当前 Windows 环境提供 PowerShell 命令。
2. 在执行当天查询 Python 官方下载页的最新稳定版本，不采用 alpha、beta、rc。交接准备时官方页面列出 Python 3.14.7，3.15 仍为预发布；执行时必须重新核实。
3. 安装或复用该稳定版解释器，创建项目专用 `.venv`，不覆盖其他项目环境。安装权限不足时说明具体失败步骤与用户需要完成的操作。
4. 检查 numpy、pandas、scipy、scikit-learn、openpyxl、matplotlib、seaborn、xgboost、shap、joblib、pytest 的稳定版本兼容性并安装。按实际需要选择版本，不能预先捏造已经验证的依赖组合。
5. 若最新解释器无法运行完整依赖栈，记录实际错误；保留最新解释器安装，另建采用最新兼容稳定版 Python 的实验环境。报告解释器版本差异及原因，不能静默降级。
6. 保存 `python --version`、解释器路径、包版本、依赖锁定文件或完整 freeze，以及 `python -m pip check` 结果。进行核心库导入及一次小规模训练检查后，再启动全量实验。

## 4 函数与工程结构

实现可从命令行重复运行的项目，不只提供 notebook 或伪代码。建议结构如下，可做等价的简化：

```text
inputs/                  原始附件
src/                     数据、模型、调参、评估、绘图、解释模块
tests/                   数据契约、切分及预测一致性检查
data/processed/          合并数据和来源映射
results/                 各折指标、预测、参数、图像和日志
models/                  完整预处理加模型管线
report/                  中文实验报告
config.json              随机种子、切分、搜索空间、资源预算
main.py                  一键实验入口
predict.py               加载模型进行新样本预测
README.md                安装、运行和复现说明
requirements-lock.txt    实際环境锁定信息
```

建议核心函数包括 `load_dataset()`、`validate_dataset()`、`build_pipeline()`、`get_search_space()`、`run_nested_cv()`、`compute_metrics()`、`plot_curves()`、`explain_model()`、`save_artifacts()`、`predict_new_samples()`。合理合并模块，不为凑数量创建空封装。参数写入配置，不在多个文件重复硬编码。

## 5 分类器与调参

实现并比较以下六种分类器，另加 `DummyClassifier(strategy='prior')` 作为基线。每种真实分类器同时保留未调参基线与调参结果，两者使用完全相同的外层切分。

| 模型 | 建议搜索参数与初始范围 |
| --- | --- |
| Logistic Regression | C：0.01、0.1、1、10、100；使用当前版本支持的 L2 配置；设置足够 max_iter |
| SVM | C：0.1、1、10、100；linear 或 rbf；rbf 的 gamma：scale、0.001、0.01、0.1；条件搜索避免无意义组合 |
| Gaussian Naive Bayes | var_smoothing：1e-11、1e-9、1e-7、1e-5、1e-3 |
| KNN | n_neighbors：1、3、5、7；weights：uniform、distance；p：1、2；邻居数不得超过最小内层训练集大小 |
| Random Forest | n_estimators：100、300；max_depth：2、3、5、None；min_samples_leaf：1、2、3；max_features：sqrt、1.0 |
| XGBoost | n_estimators：50、100、200；max_depth：1、2、3；learning_rate：0.03、0.1；subsample：0.8、1.0；colsample_bytree：0.8、1.0；reg_lambda：1、10 |

以上是为小数据设定的起始搜索范围，不是宣称最优参数。小空间使用 GridSearchCV，大空间使用固定随机种子的 RandomizedSearchCV，每种模型每个外层折最多约 20 个候选，预算和实际拟合数必须记录。不要对 27 个样本无约束扩大搜索。

预处理使用 Pipeline：数值缺失填补（如需要）及适当缩放。LR、SVM、KNN 至少使用 StandardScaler，树模型无需缩放。所有拟合型预处理均在训练折内完成。默认不做特征筛选、SMOTE 或基于全数据分布的异常值删除。出现极值应报告并核对，不能因影响得分而删样本。

SVM 为 ROC/PR 输出决策分数或训练内得到的概率，不能用硬标签作为曲线输入。若采用概率校准，校准只能发生在当前训练范围内，并检查子折样本量。决策分数不能写成概率。其他模型概率列必须按 `classes_` 对齐固定类别顺序。

XGBoost 使用当前版本支持的三分类设置（例如 multi:softprob、num_class=3），不能沿用已删除参数。若启用 early stopping，验证数据必须来自当前训练折内部，不能用外层测试折。

固定随机种子，例如 42；记录随机模型和搜索随机种子。限制并发，避免搜索器与树模型嵌套占满 CPU。记录训练警告、收敛警告、失败组合和耗时；失败项不得用 0 代替成功指标。

## 6 评估方案与泛化边界

本项目只有 27 条样本，采用外层 5 折、内层 3 折的分层嵌套交叉验证作为主实验。外层使用打乱且种子固定的 StratifiedKFold；内层在每个外层训练集上独立调参。所有模型共享同一组外层样本索引。

内层主评分预先固定为 macro-F1。外层只评估预测，不参与超参数、预处理、特征、阈值或搜索范围选择。每条样本对每个模型恰好有一次外层折外预测（OOF），保存样本 ID、真实标签、预测标签、三个类别分数、外层折编号。未调参与调参版本分别保存。

不要先用全数据调好参数再用五折结果报告泛化性能。不要在全数据上先标准化。不要为了获得高分反复挑选随机种子。外层折外结果是内部验证，不称为独立外部测试集；本项目默认不另切仅有约五六条样本的固定测试集。

为分析切分稳定性，可预先固定另两个种子 43、44，完整重复嵌套流程，将每次重复的 OOF 指标和范围作为补充结果。主结果仍使用 42，不能挑最好种子替代。重复切分不是新增独立样本，不能把 81 次预测当作 81 名患者来缩小置信区间。

各模型按预先确定的 macro-F1 比较，同时单独标记 Accuracy 最高模型以满足附件“最高精度模型”的要求。若两者不同，说明取舍，并输出两个模型的混淆矩阵。并列时依次比较 Accuracy、模型简洁性，不能事后更改规则追逐结论。

对模型家族比较后的“获胜模型”得分明确说明存在选择乐观性；各模型的嵌套评估不能自动变成获胜模型选择过程的无偏评估。不能声称当前数据已证明临床部署有效或外部人群泛化良好。

模型选择完成后，可以在全部 27 条样本上重新进行训练内调参并拟合最终模型用于演示预测。最终模型的训练集得分不得替代之前的 OOF 性能。

## 7 指标与图表

结果表列出每种模型、是否调参、外层各折结果，以及均值与标准差。另列完整 OOF 预测计算的汇总值，明确其与折均值不同。标准差是跨折变动，不能直接当作置信区间或临床不确定性范围。

必需指标：

- Accuracy。
- Precision、Recall、F1，主表使用 macro 平均；补充逐类结果、support 和 weighted 平均。
- ROC-AUC：三分类 OvR，各类 AUC 和 macro 平均。
- mAP：每类 one-vs-rest 的 average precision（AP）取算术平均。
- macro PR-AUC：另外使用 PR 曲线梯形积分，以对应附件的“PR 曲线下面积”；明确其与 AP 的算法不同。
- 训练与预测耗时，以及训练分数和外层分数的差距，用于辅助观察过拟合。

若某类指标无法计算，记录 NA 和原因。Precision/F1 因没有该类预测而为零时可采用明确的 zero_division 策略并解释，不能掩盖类别缺失或模型失败。

必需图表：类别样本数图、模型指标对比图、六模型 macro ROC 对比、六模型 macro PR 对比、逐类 ROC/PR、最佳模型混淆矩阵（原始计数及按真实类别行归一化）、SHAP 特征重要性图。基线可一同展示。

ROC/PR 只用 OOF 分数计算；采用三类共同网格插值取 macro 曲线时说明规则，PR 插值前处理 recall 的方向和重复值。图例中的 macro-AUC、mAP 分别来自逐类数值指标平均，不将平均曲线的面积混称为 mAP。跨折汇总分数的曲线是辅助展示，尤其 SVM 决策分数可能存在折间尺度差异，应同时保留逐折指标。

混淆矩阵横轴为预测类别，纵轴为真实类别；使用统一的低危、中危、高危顺序。单次主实验矩阵计数合计应为 27。

SHAP 至少解释最终所选模型，输出每类贡献与三类聚合 mean(abs(SHAP)) 排名。支持时使用适合模型的解释器，否则采用模型无关解释器并记录背景样本、解释对象、输出空间和计算预算。若解释 Pipeline，确保输入变换及显示特征含义一致。

默认可将全部样本重新拟合模型的 SHAP 用作训练后描述性分析，图表标题须说明这一点，不能称为外部验证解释。SHAP 不作为证明因果关系的证据，也不能看完全部 OOF/SHAP 后挑特征并继续沿用原分数。解释器故障要明确记录，不能把其他重要性方法冒称 SHAP。

## 8 应交付内容与验收

交付可运行源代码、环境安装与复现说明、配置和锁定依赖、数据审计记录、每折超参数和搜索结果、OOF 明细、指标 CSV、图表、可加载的完整模型管线、标签映射及特征顺序、中文实验报告。推荐报告使用 Markdown 与 HTML，便于复现。

README 必须提供实际验证过的安装命令、一键运行命令及新样本预测示例。预测输入只含同名的 10 个特征；缺列、多列、非数值和异常列顺序要明确校验或安全重排。输出类别及可获得的各类概率／决策分数，不能把分数标成疾病发生概率。

运行验收至少包含：

1. 解析后为 27 × 10 个特征，三类各 9 条；原始附件哈希不变。
2. 五个外层测试折互不重叠，合并后覆盖全部样本，每折包含三类；内外层训练测试索引不交叉。
3. 数据来源、标签、行号及样本 ID 没有进入 X；预处理在 Pipeline 内。
4. 六种模型与 Dummy 基线均真实运行成功；不以模拟值补齐未运行模型。
5. 每个模型版本 OOF 预测数量为 27，类别分数列序一致，概率（若有）有限且行和接近 1。
6. 从保存的 OOF 文件重新计算的指标，与报告的 OOF 汇总指标一致；混淆矩阵计数合计为 27。
7. 保存后重新加载模型，预测与保存前一致；错误输入能给出明确错误。
8. 实际执行最小测试与完整主实验，检查报告数字与图像。记录未完成项及其实际原因，不提前宣称通过。

报告回答：哪种模型在当前方案下表现较好；调参带来什么变化；错误主要出现在哪类；表现对切分是否敏感；哪些特征影响预测；27 条样本和未知患者身份带来哪些限制。不得承诺固定准确率，不得将训练精度当泛化性能。

## 9 参考资料

以下官方资料于交接准备时核对。执行时以最新稳定 API 为准。

- [Python 官方下载与发布版本](https://www.python.org/downloads/)
- [scikit-learn 嵌套交叉验证示例](https://scikit-learn.org/stable/auto_examples/model_selection/plot_nested_cross_validation_iris.html)
- [scikit-learn 数据泄漏与 Pipeline](https://scikit-learn.org/stable/common_pitfalls.html)
- [scikit-learn average_precision_score 定义](https://scikit-learn.org/stable/modules/generated/sklearn.metrics.average_precision_score.html)
