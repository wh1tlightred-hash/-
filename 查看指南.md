# 中医声诊项目查看指南

这是 27 条声诊样本的三分类机器学习实验项目。先打开 `report/实验报告.html` 查看方法、指标、图表和限制；需要复现时阅读 `README.md`。

| 内容 | 路径 |
| --- | --- |
| 中文实验报告 | `report/实验报告.html`、`report/实验报告.md` |
| 原始实验说明与数据 | `inputs/中医声诊实验.docx`、`inputs/声诊练习.xlsx` |
| 数据来源映射 | `data/processed/` |
| 三个种子的折外预测和指标 | `results/seed42/`、`results/seed43/`、`results/seed44/` |
| 切分与搜索候选审计 | `results/search_audit/` |
| 图表 | `results/figures/` |
| 最终演示模型及特征顺序 | `models/final_pipeline.joblib`、`models/final_metadata.json` |
| 运行代码与测试 | `main.py`、`predict.py`、`src/`、`tests/` |
| 环境重建 | `README.md`、`requirements-lock.txt` |

主实验在 27 条样本、外层五折内层三折的设置下，按折外 macro-F1 选出的模型为随机森林（macro-F1 0.5386、Accuracy 0.5556）。其高危类召回率只有 2/9；这些数字不构成临床使用或外部泛化的证据。XGBoost 在本次调参模型中取得最高的 macro ROC-AUC 与 mAP。详细逐类结果、统计限定和 SHAP 解释请看报告。

仓库不包含本机 `.venv/`。在 Windows 上安装 Python 3.14 后，按 `README.md` 使用 `requirements-lock.txt` 创建环境，再运行：

```powershell
.\.venv\Scripts\python.exe -m pytest -q
.\.venv\Scripts\python.exe verify_acceptance.py
.\.venv\Scripts\python.exe main.py
```

`main.py` 会重建数据、结果、图和报告。原始附件保留在 `inputs/`，不要用生成结果替代它们。
